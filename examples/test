Forward(Init){
	*(Init): Closest	 & True ? Move(F) : (Init)
}

Virus(Init){

    *(Init):
    |Key(o) ? Move(N) :(Init)

    |Key(m) ? Move(E) :(Init)

    |Key(l)? Move(S) :(Init)

    |Key(k) ? Move(O) :(Init)

    |Key(j) ? Pick() :(Init)
    |Key(n) ? Get() :(Init)
    |Key(p) ? Pop() :(Init)
    |Key(i) ? Wizz() :(Init)
    |Key(y) ? Hit() :(Init)
    |True ? Jump() :(Init)
    
}

Antivirus(Init){

    *(Init):
    |Key(z) ? Move(N) :(Init)
    |Key(s)? Move(S) :(Init)
    |Key(q) ? Move(O) :(Init)
    |Key(f) ? Move(E) :(Init)

    |Key(g) ? Pick() :(Init)
    |Key(v) ? Get() :(Init)
    |Key(r) ? Pop() :(Init)
    |Key(a) ? Wizz() :(Init)
    |Key(c) ? Hit() :(Init)
    |True ? Jump() :(Init)

}

